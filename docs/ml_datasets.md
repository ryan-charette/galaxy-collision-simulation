# ML Dataset Schemas

Generated ML datasets are versioned tabular artifacts written by
`scripts/generate_ml_dataset.py` and `scripts/generate_residual_dataset.py`.
Every schema includes `dataset_schema_version = "0.1.0"` so model artifacts can
record which dataset contract they were trained against.

Dataset generation writes two tables for each dataset type:

- `*.raw.*`: every generated row, including failed or incomplete runs.
- `*.*`: cleaned rows with completed status and no missing required values.

Each dataset also receives `*.manifest.json` and `*.summary.md` sidecar files.

## Solver Tuning

One row represents one simulator run or segment.

Required columns:

```text
dataset_schema_version
run_id
git_commit
config_sha256
hardware_type
solver
n_particles
steps
dt
softening
tree_theta
tree_leaf_capacity
fmm_expansion_order
output_format
median_step_time
total_wall_time
particle_steps_per_second
energy_drift_final
momentum_drift_final
max_energy_drift
max_momentum_drift
```

## Force Error

One row represents one approximate solver/config comparison against a matching
direct run.

Required columns:

```text
dataset_schema_version
run_id
direct_run_id
git_commit
config_sha256
direct_config_sha256
solver
n_particles
tree_theta
tree_leaf_capacity
fmm_expansion_order
softening
force_rmse
force_mae
force_max_error
relative_force_rmse
runtime_direct
runtime_approx
speedup_vs_direct
```

## Per-Step Diagnostics

One row represents one diagnostics entry. To capture every simulation step, use
a sweep where `simulation.snapshot_every = 1`.

Required columns:

```text
dataset_schema_version
run_id
git_commit
config_sha256
solver
n_particles
step
time
kinetic_energy
potential_energy
total_energy
linear_momentum_x
linear_momentum_y
linear_momentum_z
angular_momentum_x
angular_momentum_y
angular_momentum_z
step_wall_time
```

`step_wall_time` is currently the run-level average wall time per configured
step because the simulator does not yet emit per-step timing samples.

## Acceleration Residuals

One row represents one particle in a paired direct-vs-approximate step-0 force
comparison. These rows are generated by `scripts/generate_residual_dataset.py`.

Required columns:

```text
dataset_schema_version
residual_schema_version
run_id
reference_run_id
config_id
git_commit
config_sha256
direct_config_sha256
solver
n_particles
softening
tree_theta
tree_leaf_capacity
fmm_expansion_order
particle_id
position_x
position_y
position_z
velocity_x
velocity_y
velocity_z
mass
group_id
direct_accel_x
direct_accel_y
direct_accel_z
approx_accel_x
approx_accel_y
approx_accel_z
accel_error_x
accel_error_y
accel_error_z
local_density_estimate
nearest_neighbor_distance
distance_from_center_of_mass
leaf_particle_count
tree_depth
cell_size
distance_to_cell_center
```
