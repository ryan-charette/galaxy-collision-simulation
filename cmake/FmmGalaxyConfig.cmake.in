@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@HAVE_MPI@)
    find_dependency(MPI)
endif()

if(@HAVE_OPENMP@)
    find_dependency(OpenMP)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/FmmGalaxyTargets.cmake")

check_required_components(FmmGalaxy)
