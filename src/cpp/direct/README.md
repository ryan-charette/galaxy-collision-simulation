# Direct Solver

This module implements the `O(N^2)` softened-gravity baseline.

Responsibilities:

- pairwise Plummer-softened acceleration,
- exact pairwise symmetry using Newton's third law,
- direct baseline for tree/FMM error comparison,
- small-system validation tests.
